// this merely re-exports as the default export. however, it does also mean we could
// globally change the asset source if we wanted to.

import MediaAssetSource from 'part:sanity-plugin-media/asset-source'

export default [MediaAssetSource]